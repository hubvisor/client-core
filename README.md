# Hubvisor Client Core

Utility library for Hubvisor client-side projects.
Using a common library allows webpack to resolve the same library for multiple projects and produce a smaller final artifact.
